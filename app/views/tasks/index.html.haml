.container
  %h2= @board.title

  - @tasks.each do |task|
    .card
      .card_section
        .card_section_title
          = task.title
        
        .board_detail_actions
        - if user_signed_in?
          .dropdown
            = image_tag 'Combined Shape.png', class: 'dropbtn'
            .dropdown-content.mini
              = link_to 'Edit'
              = link_to 'Delete'
      .card_content  
        = task.content
      .card_detail
        = image_tag 'Oval (1).png'

  = link_to new_board_task_path do
    .new
      .new_title
        + Add new card


        -#  && current_user.has_written?(board)

        -# , edit_board_path(board)

        -# , board_path(board), data: { method: 'delete' , confirm: '本当に削除してもよろしいですか？'}